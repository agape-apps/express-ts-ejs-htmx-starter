<%# Pro-tip: Always remember to include the htmx header and footer to pages %>
<%- include('partials/_htmx-header') %>
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4">Your first htmx-app. Probably.</h1>
        <div id="content-swap">
            <button type="button" hx-get="/congrats/1" hx-swap="innerHTML" hx-target="#content-swap" class="btn btn-primary">Click Me.</button>
        </div>
        <hr class="my-4">
        <p>Created to make your dev-life easier</p>
    </div>
<%- include('partials/_htmx-footer') %>
